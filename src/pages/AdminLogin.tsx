import { useState, type FormEvent } from "react";
import { useNavigate } from "react-router-dom";
import { Header } from "../components/Shell";
import { useI18n } from "../i18n";
import { api } from "../lib/api";

export function AdminLogin(){const {t}=useI18n();const navigate=useNavigate();const[email,setEmail]=useState("");const[password,setPassword]=useState("");const[error,setError]=useState("");const[loading,setLoading]=useState(false);const submit=async(event:FormEvent)=>{event.preventDefault();setLoading(true);setError("");try{await api("/api/admin/login",{method:"POST",body:JSON.stringify({email,password})});navigate("/admin");}catch(err){setError(err instanceof Error?err.message:"LOGIN_FAILED");}finally{setLoading(false);}};return <div className="admin-login"><Header minimal/><main><div className="admin-login-copy"><p>OWNER / AUTHORIZED PERSONNEL ONLY</p><h1>CONTROL<br/>THE CASE.</h1><div className="terminal-lines"><span>ROOM ENGINE · ONLINE</span><span>D1 ARCHIVE · ENCRYPTED</span><span>R2 EVIDENCE · PRIVATE</span></div></div><form onSubmit={submit}><p className="kicker">{t("admin.login")}</p><h2>{t("admin.title")}</h2><label>{t("admin.email")}<input type="email" autoComplete="username" required value={email} onChange={e=>setEmail(e.target.value)}/></label><label>{t("admin.password")}<input type="password" autoComplete="current-password" required minLength={12} value={password} onChange={e=>setPassword(e.target.value)}/></label>{error&&<p className="form-error">{error}</p>}<button className="button button-primary full" disabled={loading}>{loading?"AUTHENTICATING…":t("admin.signin")}</button></form></main></div>;}
